#include<bits/stdc++.h>
using namespace std;

int main(){
  random_device rd;
  mt19937 gen(rd());
  uniform_int_distribution<> distr(25, 63); 

  for(int n=0; n<40; ++n)
      cout << distr(gen) << ' '; 
}
