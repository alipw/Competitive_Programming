#include <bits/stdc++.h>
using namespace std;
#define ar array

voi solve(){
	cin >> n >> sum;
	int arr[n];
	for(int a = 0; a < n; a++)
		cin >> arr[a];
	for(int a = 0; a < n; a++){
		
	}
}

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	solve();
}